# --- www -> apex (preserve existing canonical host redirect) ---
https://www.ebusinessinnovators.com/*   https://ebusinessinnovators.com/:splat   301!

# --- legacy .html paths from the old single-file site ---
/index.html      /            301
/privacy.html    /privacy/    301
/404.html        /404.html    200

# NOTE: old hash deep-links like /#about are handled client-side in site.js
# (the browser never sends the # fragment to the server, so they cannot be
#  redirected here). See the "legacy hash" block in assets/js/site.js.

# --- fallback: unknown paths -> branded 404 ---
/*   /404.html   404
